import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ApparelComp = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  )
}

export const Apparel = React.memo(ApparelComp)

const styles = StyleSheet.create({})