import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TshirtComp = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  )
}

export const Tshirt = React.memo(TshirtComp)

const styles = StyleSheet.create({})